import 'package:flutter/material.dart';

// TODO : Gaith for Drawer
AppBar mainAppbar = AppBar(
        actions: [
          Image.asset("assets/images/logo.png")
        ],
        leading: IconButton(
          onPressed: () {},
          icon: const Icon(Icons.menu),
        ),
      );